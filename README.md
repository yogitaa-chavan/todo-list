# todo-list

# Spring boot <br>
  Dependencies<br>
  -web<br>
  -mysql Driver<br>
  -thymleaf<br>
  -lombok (To reduce boiler plate code)<br><br>

# Packages and classes<br>
-Controller<br>
  -TaskController (Handels http request)<br>
-Services<br>
  -TaskServices (create methods for delete, toggle,createtask, gettask)<br>
-Repository<br>
  -TaskRepository (extends JpaRepository ( gives built-in database operations (like save(), findAll(), deleteById(), etc.)))<br>
-models<br>
  -task (create structure or declaration of variables )
